<nav class="navbar navbar-expand-lg navbar-light py-0 px-2" style="background-color: rgba(255,255,255,0.9)">
    <div class="container-fluid px-2">
        <a class="navbar-brand" href="#">
            <img class="img-fluid" style="height: 40px; object-fit: cover" src="~/images/mora_maths_logo_wide.png" alt="">
        </a>
        <!-- Add your navigation menu items here -->
        <button class="navbar-toggler order-1" type="button" data-toggle="collapse" 
                            data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
        <div class="collapse navbar-collapse order-3" id="navbarCollapse">
            <!-- Left navbar links -->
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a asp-action="Instructions" asp-controller="Quiz" class="nav-link">Start New Quiz</a>
                </li>
                <li>
                    <a asp-action="Leaderboard" asp-controller="Quiz" class="nav-link">Leaderboard</a>
                </li>
                @if(User.IsInRole("Admin"))
                {
                    <li class="nav-item">
                        <a asp-action="ListRoles" asp-controller="Administration" class="nav-link">Roles</a>
                    </li>
                }
                @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                {
                    <li class="nav-item">
                        <a asp-action="List" asp-controller="Questions" class="nav-link">Questions</a>
                    </li>
                }
            </ul>
            @* if user logged in *@
            @if (User.Identity is {IsAuthenticated: true })
            {
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a asp-action="Logout" asp-controller="Account" class="btn btn-info">Logout</a>
                    </li>
                </ul>
            }
        </div>
    </div>
</nav>

